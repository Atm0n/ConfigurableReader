<Window x:Class="ConfigurableReader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Single Line Reader" Height="Auto" Width="Auto" Closing="Window_Closing" KeyDown="Window_KeyDown">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Background" Value="LightGray"/>
        </Style>
        <Style TargetType="Slider">
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="xctk:IntegerUpDown">
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="xctk:ColorPicker">
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="10">
            <Button Content="Open File" Click="OpenFileButton_Click" />
            <Button x:Name="StartStopButton" Content="StartStop" Click="StartStopButton_Click" />
            <TextBlock Text="Speed:" VerticalAlignment="Center" />
            <Slider x:Name="SpeedSlider" Minimum="0" Maximum="10" ValueChanged="SpeedSlider_ValueChanged" Width="300" />
            <xctk:ColorPicker x:Name="ColorPicker" SelectedColorChanged="ColorPicker_SelectedColorChanged" />
            <xctk:ColorPicker x:Name="BackgroundColorPicker" SelectedColorChanged="BackgroundColorPicker_SelectedColorChanged" />
            <TextBlock Text="Font Size:" VerticalAlignment="Center" />
            <xctk:IntegerUpDown x:Name="FontSizeSlider" AllowTextInput="True" Minimum="10" Maximum="1000" ValueChanged="FontSizeSlider_ValueChanged" Width="100" />
        </StackPanel>
        <Grid Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <ScrollViewer x:Name="ScrollViewer" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled">
                <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock x:Name="TextBlock" TextWrapping="NoWrap" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"/>
                </Grid>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
